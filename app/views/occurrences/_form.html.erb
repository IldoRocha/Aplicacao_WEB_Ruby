<%= form_with(model: occurrence, local: true) do |form| %>
  <% if occurrence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(occurrence.errors.count, "error") %> prohibited this occurrence from being saved:</h2>

      <ul>
      <% occurrence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :person %>
    <%= collection_select(:occurrence, :person_id, Person.all, :id, :name) %>
  </div>
  
  <div class="field">
    <%= form.label :state %>
    <%= collection_select(:occurrence, :state_id, State.all, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :cobradeCode %>
    <%= form.text_field :cobradeCode %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
